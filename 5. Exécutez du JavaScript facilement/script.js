// # Exécutez du JavaScript facilement

// ## Découvrez l’avantage de Java Script : un langage, de nombreux environnement

// Découvrez JSBin

// Pour tester des extraits de code en JS. 

// Dans JSBin on peut écrire du code ligne par ligne dans le volet JS, et utiliser le bouton Run pour l’exécuter. JSBin parcours lignes de code et les exécute l’une après l’autre.

// ### Écrivez du code pour les pages web

// On écrit du code dans un fichier, puis on l’importe dans une balise spéciale  dans notre fichier html. Le navigateur exécute ensuite le code dans ces fichiers, en gl dans l’ordre de leur importation. 

// ### Écrivez du JavaScript pour des serveurs

// Avt : JS ne pouvait s’utiliser que dans des pages web (en avant-plan). 

// Ajd’hui : il peut s’utiliser en arrière plan pour gérer l’accès à certaines ressources. 

// Ex : service de messagerie : messages ne sont pas enregistrés dans la page mais dans une base de données. La page qu’on utilise accède à cette base de données à travers un serveur qui peut ê écrit en JS. 

// Les serveurs doivent être actifs à tous moment et seuls certains élé de code doivent ê exécutés à certains moments. Ex : quand on décide d’ouvre le client de messagerie. 

// Dans ce cas un environnement (comme Node) couplé à un code JS fait que cela se produit. Le serveur réagit à votre ouverture  du client, vérifie l’authentification et envoie vos messages.